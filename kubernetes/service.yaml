apiVersion: v1
kind: Service
metadata:
  name: medicure
  namespace: medicure
  labels:
    app: medicure
spec:
  selector:
    app: medicure
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
